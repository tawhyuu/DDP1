# def jaka():
#     total_kombinasi = 0
#     semua_kombinasi = []
#     for a in range(1, 12):  # maksimal 11
#         for b in range(a+1, 13):  # a<b maks 12
#             for c in range(b+1, 14):  # a<b<c maks 13
#                 for d in range(c+1, 15):  # maks 14
#                     for e in range(d+1, 16):  # maks 15
#                         kombinasi = [a, b, c, d, e]
#                         semua_kombinasi.append(kombinasi)
#                         total_kombinasi += 1
    
#     print(f"Jumlah kombinasi list yang mungkin: {total_kombinasi}")
#     print(f"Jumlah kombinasi list yang mungkin: {len(semua_kombinasi)}")

# jaka()

print(969*(-8)*43046721)