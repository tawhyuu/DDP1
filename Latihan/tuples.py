# def who_follows(data, name):
    # result = []
    # for (follower, followee) in data:
    #     if followee == name:
    #         result.append(follower)
    # return result
#     return [follower for(follower, followee) in data if followee == name]

# data = [('ani', 'anto'), ('rio', 'anto'), ('budi', 'jesica')]
# print(who_follows(data, 'anto'))

a = 'a'
b = a
print(a is b)